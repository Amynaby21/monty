#include "monty.c"

/**
 * _nop - Nothing to do
 * @stack: A pointer to the top of the stack
 * @line_number: Line number of the bycode
 *
 */

void _nop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
